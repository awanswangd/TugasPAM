CREATE TABLE NoteEntity (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    content TEXT NOT NULL,
    createdAt INTEGER NOT NULL
);

getAllNotes:
SELECT *
FROM NoteEntity
ORDER BY createdAt DESC;

getNoteById:
SELECT *
FROM NoteEntity
WHERE id = :id;

insertNote:
INSERT OR REPLACE
INTO NoteEntity (id, title, content, createdAt)
VALUES (?, ?, ?, ?);

deleteNoteById:
DELETE FROM NoteEntity
WHERE id = :id;

searchNotes:
SELECT *
FROM NoteEntity
WHERE title LIKE ('%' || :query || '%') OR content LIKE ('%' || :query || '%')
ORDER BY createdAt DESC;
